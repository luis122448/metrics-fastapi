# Copy this file to .env and fill in the values before running locally.
# Never commit .env — it must be listed in .gitignore.

# Application
APP_ENV=
APP_TIMEZONE=

# Database (SQLite path override, if needed)
# DATABASE_PATH=
