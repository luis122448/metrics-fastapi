services:
  app:
    container_name: metrics-fastapi
    env_file:
      - ./.env
    build: 
      context: .
      dockerfile: Dockerfile
    command: "python app/server.py"
    volumes:
      - ${HOST_DATABASE_DIR}:/opt/database
    ports:
      - "8083:8083"
    restart: always